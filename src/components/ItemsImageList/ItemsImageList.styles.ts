import styled from "styled-components"

export const ItemsImageListWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(30%, 1fr));
  grid-column-gap: 40px;
  grid-row-gap: 40px;
`
